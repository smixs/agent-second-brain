[Unit]
Description=Second brain — nightly processing (21:00 local)

[Timer]
OnCalendar=*-*-* 21:00:00
# Catch up after downtime (VPS reboot, laptop asleep) instead of skipping.
Persistent=true

[Install]
WantedBy=timers.target
